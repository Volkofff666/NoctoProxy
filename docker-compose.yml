services:
  bot:
    build: .
    container_name: tg-proxy-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/data/bot.db
      - PROXIES_PATH=/app/config/proxies.json
    volumes:
      - bot_data:/data
      - ./config:/app/config

volumes:
  bot_data:
